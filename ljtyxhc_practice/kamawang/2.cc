#include<iostream>
using namespace std;
int main()
{
    long long a,b,n;
    while(cin>>n)
    while (n--)
    {
        cin>>a>>b;
        cout<<a+b<<endl;
    }
    return 0;
    //错误原因：输入n不止一组，要持续的看n的值是否输入
};